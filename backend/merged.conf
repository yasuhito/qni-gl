server {
  listen 8000;
  root /qni-gl/vite/dist;
  index index.html index.htm;

  location / {
    # auth_basic "Restricted";
    # auth_basic_user_file /etc/nginx/.htpasswd;

    location /backend.json {
      proxy_pass http://unix:/tmp/gunicorn.sock;
    }
  }
}
